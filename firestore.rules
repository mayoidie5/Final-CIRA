rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Helper function to check if a user is an admin
    function isAdmin(userId) {
      return exists(/databases/$(database)/documents/users/$(userId)) &&
             get(/databases/$(database)/documents/users/$(userId)).data.role == 'admin';
    }

    // Helper function to check if a user is a class representative
    function isClassRepresentative(userId) {
      return exists(/databases/$(database)/documents/users/$(userId)) &&
             (get(/databases/$(database)/documents/users/$(userId)).data.role == 'class_rep' ||
              get(/databases/$(database)/documents/users/$(userId)).data.role == 'class-representative');
    }

    // Rules for the 'users' collection
    match /users/{userId} {
      allow create: if request.auth != null;
      allow read, update: if request.auth != null && request.auth.uid == userId;
      allow read, write: if request.auth != null && isAdmin(request.auth.uid);
    }

    // Rules for the 'tickets' collection
    match /tickets/{ticketId} {
      allow create: if request.auth != null;
      allow read, write: if request.auth != null && (isAdmin(request.auth.uid) || isClassRepresentative(request.auth.uid));
      allow read, update: if request.auth != null && request.auth.uid == resource.data.userId;
      allow delete: if request.auth != null && request.auth.uid == resource.data.userId;
    }

    // Rules for the 'form-structure' collection
    match /form-structure/{fieldId} {
        allow read: if request.auth != null;
        allow create, update, delete: if request.auth != null && isAdmin(request.auth.uid);
    }

    // Rules for the 'notifications' collection
    match /notifications/{notificationId} {
      allow read: if request.auth != null && request.auth.uid == resource.data.userId;
      allow create, update, delete: if request.auth != null && isAdmin(request.auth.uid);
    }

    // Rules for the 'formConfig' collection (for form editor)
    // Allow all authenticated users to read
    // Allow create, update, delete only to admins
    match /formConfig/{document=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && isAdmin(request.auth.uid);
    }
    
    // Catch-all deny rule for any other paths
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
